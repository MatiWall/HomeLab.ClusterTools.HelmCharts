---
apiVersion: v2
name: deployment-template # deployable unit
appVersion: "1.0"
description: "Deployment template"
version: 0.1.4
type: application
dependencies:
  - name: common
    version: 0.0.3
    repository: "file://../common"  # Adjust the path as needed
